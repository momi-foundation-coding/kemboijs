/**
 * Getting data from request and passing them to response
 * Then, send back to the user
 */

const response = response => {
    console.log('This is my response coming here', response)
}

module.exports = response;
